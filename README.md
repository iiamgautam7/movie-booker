# Movie Booker - Automation Demo

## Overview
Mock movie booking site (Flask) + Selenium worker which automates booking requests written to the DB. CI/CD pipeline via Jenkins.

## Quick start (local)
1. Create venv and install: